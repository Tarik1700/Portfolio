import Spline from "@splinetool/react-spline";
import React from "react";

const Background = (props) => {
  return (
    <div>
          <Spline scene={props.name} />
	
	</div>
  )
}

export default Background
